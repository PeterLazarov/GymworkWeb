query WorkoutsByDate($date: ISO8601Date!) {
  workouts(date: $date) {
    id
    date
    steps {
      id
      exercises {
        id
        name
      }
      sets {
        id
        reps
        weightMcg
      }
    }
  }
}
