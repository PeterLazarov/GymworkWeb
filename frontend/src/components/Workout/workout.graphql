query WorkoutsByDate($date: ISO8601Date!) {
  workouts(date: $date) {
    id
    date
    steps {
      id
      exercises {
        id
        name
        measurements
      }
      sets {
        id
        reps
        weightMcg
      }
    }
  }
}
