query ExerciseSets($exerciseId: ID!, $period: String) {
  settings {
    theme
  }
  exercise(id: $exerciseId) {
    id
    measurements {
      ...MeasurementsFragment
    }
    steps(period: $period) {
      id
      date
      sets {
        ...WorkoutSetFragment
      }
    }
  }
}
