# import './WorkoutSetFragment.graphql'

query ExerciseSets($exerciseId: ID!) {
  exercise(id: $exerciseId) {
    id
    steps {
      id
      sets {
        ...WorkoutSetFragment
      }
    }
  }
}
