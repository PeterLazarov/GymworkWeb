query ExerciseSets($exerciseId: ID!) {
  exercise(id: $exerciseId) {
    id
    measurements {
      ...MeasurementsFragment
    }
    steps {
      id
      date
      sets {
        ...WorkoutSetFragment
      }
    }
  }
}
