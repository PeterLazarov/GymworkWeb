mutation CreateWorkout($date: ISO8601DateTime!) {
  createWorkout(input: { date: $date }) {
    workout {
      id
      date
      steps {
        id
        exercises {
          id
          name
        }
        sets {
          id
          reps
          weightMcg
        }
      }
    }
    errors
  }
}

